/**
 * Crear un componente de tarjeta de precios de la luz
 */

export const PriceCard = (data) => {
    const card = document.createElement("div");
    card.classList.add("card");
    card.innerHTML
}